<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yahait.app.Dao.IDao">

<select id="loigndate" resultType="com.yahait.app.Dto.MemberDto">
SELECT id,password,name FROM member
</select>

<select id="logincehck" resultType="com.yahait.app.Dto.MemberDto">
SELECT id,password FROM member where id = #{id} 
</select>

</mapper>