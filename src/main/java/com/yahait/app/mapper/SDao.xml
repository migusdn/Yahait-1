<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yahait.app.Dao.SDao">
	<select id="Shop_info" parameterType="String" resultType="com.yahait.app.Dto.ShopDto">
	SELECT * FROM shop WHERE shop_num =#{shop_num}
	</select>
	<select id="Shop_admin" parameterType="String" resultType="com.yahait.app.Dto.ShopDto">
	SELECT member_num FROM member WHERE member_id=#{member_id}
	</select>
	<insert id="Shop_add" parameterType="map">
	INSERT INTO shop (member_num,shop_name, category_name1, category_name2,state, shop_info, shop_pic)
	VALUES(#{member_num},#{shop_name},#{category_name1},#{category_name2},1, #{shop_info}, #{shop_pic})
	</insert>
	<select id="Shop_list" parameterType="String" resultType="com.yahait.app.Dto.ShopDto">
	SELECT * FROM shop WHERE member_num=#{member_num}
	</select>
	<update id="StateUpdate" parameterType="map">
	UPDATE shop SET state = #{state} WHERE shop_name =#{shop_name}
	</update>
	<select id="Shop_show" resultType="com.yahait.app.Dto.ShopDto">
	select shop_name , shop_pic from shop
	</select>
	<select id="name_duplication_check" parameterType="String"
		resultType="com.yahait.app.Dto.ShopDto">
		SELECT shop_name FROM shop where shop_name = #{shop_name}
	</select>
</mapper>